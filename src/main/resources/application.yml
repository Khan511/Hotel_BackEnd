# spring.application.name=gjensidige-sHotel
server:
  port: 9192

spring:
  datasource:
    username: springstudent
    password: ********
    url: jdbc:mysql://localhost:3306/gjensidige_hotel
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true
      dialect: org.hibernate.dialect.MySQLDialect  # Add this line

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
      file-size-threshold: 2KB
